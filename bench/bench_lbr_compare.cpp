#include <cstdio>

int main(){
  std::puts("The external comparison benchmark has been retired.");
  std::puts("volfi is maintained as a research reference implementation of the quantile identity.");
  std::puts("Use bench_otm_grid for same-codebase local regression diagnostics only.");
  return 0;
}
