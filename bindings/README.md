# Bindings

Language translations and wrappers for the flagship C++ `volfi` core live here.

Current contents:

- `R/volfiR`: R package translation built with `Rcpp`

The repository remains C++-first. Additional language ports such as Python can be added alongside the R package under this directory without changing the core layout.
